version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "5003:5003"
    container_name: backend-service

  frontend:
    build: ./frontend/app
    ports:
      - "3000:80"
    depends_on:
      - backend
    container_name: frontend-service